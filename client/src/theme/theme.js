import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    heart: {
      200: "#ED4956",
    },
    feedBackground: {
      300: "#FAFAFA",
    },
  },
});

export default theme;
